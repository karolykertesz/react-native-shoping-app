class CartItem {
  constructor(quantity, price, title, total) {
    (this.quantity = quantity), (this.price = price);
    (this.title = title), (this.total = total);
  }
 
}

export default CartItem;
